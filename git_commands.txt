email id to be used for commits

git config --global user.name "your user name"
git config --global user.email	"your email id"

git config --list -> list all the git config


git init -> initializes a folder to be used in an git environment.

git remote add origin "link to use as origin for git.git"

git add . -> adds all the new file in the folder

git add -u -> updates the changes done to the files which are being tracked by git

git add -A -> adds and updates files

git commit -m "message" -> commits all the changes with the message.

gti checkout -b branchname -> creates a branch and moves the user to that branch

git branch -> lists all the branches created locally..

git checkout master -> switches the user to master branch


